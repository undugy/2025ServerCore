syntax = "proto3";
package Protocol;

enum EPacketProtocol
{
	PACKET_PROTOCOL_START		= 0;

	CS_ChatRequest				= 1;
	SC_ChatResponse				= 2;
	CS_GetIDRequest				= 3;
	SC_GetIDResponse			= 4;

}

enum EDBProtocol
{
	DB_PROTOCOL_START			= 0;
	SGDB_ServerStart			= 1;
	SGDB_ChatRequest			= 2;
	GDBS_ChatResponse			= 3;
}